/*package abs.countDown.prob;

public class DupStrring {

    public static void main(String[] args) {
        
      

    }
 
    public static void SearchStringCount(String value)
    {
        String result = "";
         // char[] s=value.toCharArray();
                 int count = 0;
                 for (int i = 0; i <= value.length() - 1; i++)
                 {
                        if (value[0] == value[i])
                        {
                               count++;
                        }
                 }

                 result += value[0] + " " + count + ", ";
                 value = value.Replace(value[0].ToString(), string.Empty);
           }
           System.out.println(result);
    }
}
*/